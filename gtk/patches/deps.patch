diff -Naur orig/configure patched/configure
--- orig/configure	2015-04-27 19:32:34.000000000 +0200
+++ patched/configure	2015-06-14 00:32:23.520003143 +0200
@@ -3041,7 +3041,7 @@
 
 
 GLIB_REQUIRED_VERSION=2.41.2
-PANGO_REQUIRED_VERSION=1.36.7
+PANGO_REQUIRED_VERSION=1.36.5
 ATK_REQUIRED_VERSION=2.12.0
 CAIRO_REQUIRED_VERSION=1.12.0
 GDK_PIXBUF_REQUIRED_VERSION=2.30.0
@@ -20630,12 +20630,12 @@
     pkg_cv_BASE_DEPENDENCIES_CFLAGS="$BASE_DEPENDENCIES_CFLAGS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_BASE_DEPENDENCIES_CFLAGS=`$PKG_CONFIG --cflags "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>/dev/null`
+  pkg_cv_BASE_DEPENDENCIES_CFLAGS=`$PKG_CONFIG --cflags "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -20647,12 +20647,12 @@
     pkg_cv_BASE_DEPENDENCIES_LIBS="$BASE_DEPENDENCIES_LIBS"
  elif test -n "$PKG_CONFIG"; then
     if test -n "$PKG_CONFIG" && \
-    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0\""; } >&5
-  ($PKG_CONFIG --exists --print-errors "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0") 2>&5
+    { { $as_echo "$as_me:${as_lineno-$LINENO}: \$PKG_CONFIG --exists --print-errors \"glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0\""; } >&5
+  ($PKG_CONFIG --exists --print-errors "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0") 2>&5
   ac_status=$?
   $as_echo "$as_me:${as_lineno-$LINENO}: \$? = $ac_status" >&5
   test $ac_status = 0; }; then
-  pkg_cv_BASE_DEPENDENCIES_LIBS=`$PKG_CONFIG --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>/dev/null`
+  pkg_cv_BASE_DEPENDENCIES_LIBS=`$PKG_CONFIG --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>/dev/null`
 		      test "x$?" != "x0" && pkg_failed=yes
 else
   pkg_failed=yes
@@ -20673,14 +20673,14 @@
         _pkg_short_errors_supported=no
 fi
         if test $_pkg_short_errors_supported = yes; then
-	        BASE_DEPENDENCIES_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>&1`
+	        BASE_DEPENDENCIES_PKG_ERRORS=`$PKG_CONFIG --short-errors --print-errors --cflags --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>&1`
         else
-	        BASE_DEPENDENCIES_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>&1`
+	        BASE_DEPENDENCIES_PKG_ERRORS=`$PKG_CONFIG --print-errors --cflags --libs "glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0" 2>&1`
         fi
 	# Put the nasty error message in config.log where it belongs
 	echo "$BASE_DEPENDENCIES_PKG_ERRORS" >&5
 
-	as_fn_error $? "Package requirements (glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.7    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0) were not met:
+	as_fn_error $? "Package requirements (glib-2.0 >= 2.41.2    atk >= 2.12.0    pango >= 1.36.5    cairo >= 1.12.0    cairo-gobject >= 1.12.0    gdk-pixbuf-2.0 >= 2.30.0) were not met:
 
 $BASE_DEPENDENCIES_PKG_ERRORS
 
diff -Naur orig/configure.ac patched/configure.ac
--- orig/configure.ac	2015-04-27 19:31:49.000000000 +0200
+++ patched/configure.ac	2015-06-14 00:31:45.639815293 +0200
@@ -42,7 +42,7 @@
 
 # required versions of other packages
 m4_define([glib_required_version], [2.41.2])
-m4_define([pango_required_version], [1.36.7])
+m4_define([pango_required_version], [1.36.5])
 m4_define([atk_required_version], [2.12.0])
 m4_define([cairo_required_version], [1.12.0])
 m4_define([gdk_pixbuf_required_version], [2.30.0])
diff -Naur orig/gtk/gtklabel.c patched/gtk/gtklabel.c
--- orig/gtk/gtklabel.c	2015-04-27 19:27:32.000000000 +0200
+++ patched/gtk/gtklabel.c	2015-06-14 00:44:53.479721993 +0200
@@ -60,6 +60,8 @@
  */
 #include "fallback-c89.c"
 
+#define PANGO_ANALYSIS_FLAG_IS_ELLIPSIS (1 << 1)
+
 /**
  * SECTION:gtklabel
  * @Short_description: A widget that displays a small to medium amount of text
